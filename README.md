# اپلیکیشن اخبار هوش مصنوعی | AI News App

اپلیکیشن Progressive Web App (PWA) برای نمایش آخرین اخبار و پیشرفت‌های هوش مصنوعی به زبان فارسی.

## 🚀 ویژگی‌ها

### 📱 قابلیت‌های اصلی:
- **نصبی**: قابل نصب روی موبایل و دسکتاپ مثل اپلیکیشن واقعی
- **آفلاین**: کار می‌کند حتی بدون اینترنت
- **جستجو**: جستجوی سریع در بین خبرها
- **دسته‌بندی**: فیلتر بر اساس موضوعات مختلف
- **به‌روزرسانی**: دکمه Refresh برای گرفتن خبرهای جدید
- **منابع معتبر**: خبرها از منابع معتبر AI جمع‌آوری می‌شود

### 🎯 موضوعات خبری:
- ChatGPT و OpenAI
- Machine Learning 
- Robotics (رباتیک)
- Deep Learning
- Computer Vision
- NLP (پردازش زبان طبیعی)

### 🌍 منابع خبری:
- OpenAI Blog
- MIT Technology Review AI
- Google AI Blog
- Google DeepMind
- TechCrunch AI

## 📦 نصب و راه‌اندازی

### روش 1: استفاده مستقیم در مرورگر
1. فایل‌ها را در یک سرور وب آپلود کنید
2. آدرس سایت را در موبایل یا دسکتاپ باز کنید
3. روی دکمه "📱 نصب اپ" کلیک کنید

### روش 2: لوکال هاست (برای توسعه)
```bash
# Clone کردن یا دانلود فایل‌ها
# وارد پوشه پروژه شوید

# Python 3
python -m http.server 8000

# یا Node.js
npx serve .

# یا PHP
php -S localhost:8000
```

سپس به آدرس `http://localhost:8000` بروید.

## 🔧 تنظیمات پیشرفته

### اضافه کردن API Keys واقعی:
فایل `ai-news-service.js` را باز کنید و API keys خودتان را اضافه کنید:

```javascript
this.apiKeys = {
    newsapi: 'YOUR_NEWSAPI_KEY',     // از newsapi.org
    gnews: 'YOUR_GNEWS_API_KEY'      // از gnews.io
};
```

### منابع API رایگان:
- **NewsAPI.org**: 1000 درخواست رایگان در ماه
- **GNews.io**: 100 درخواست رایگان در روز
- **NewsData.io**: 200 درخواست رایگان در روز

## 🎨 سفارشی‌سازی

### تغییر رنگ‌ها:
در فایل `index.html`، متغیرهای CSS را تغییر دهید:

```css
:root {
    --primary-500: #0A68FF;      /* رنگ اصلی */
    --primary-100: #E7F0FF;      /* رنگ روشن */
    --primary-700: #004DC8;      /* رنگ تیره */
}
```

### اضافه کردن دسته‌بندی جدید:
در HTML، چیپ جدید اضافه کنید:

```html
<button class="chip" data-category="quantum">Quantum AI</button>
```

### تغییر فونت:
در CSS، فونت Vazirmatn را با فونت مورد نظرتان جایگزین کنید.

## 📱 استفاده در موبایل

### نصب روی اندروید:
1. سایت را در Chrome باز کنید
2. روی منو (⋮) کلیک کنید
3. "Add to Home screen" را انتخاب کنید
4. نام اپ را تایید کنید

### نصب روی iOS:
1. سایت را در Safari باز کنید
2. روی دکمه Share (📤) کلیک کنید
3. "Add to Home Screen" را انتخاب کنید

## 🔄 به‌روزرسانی خودکار

اپلیکیشن به صورت خودکار:
- هر 30 دقیقه cache را چک می‌کند
- در صورت آفلاین بودن، از cache استفاده می‌کند
- در صورت آنلاین بودن، خبرهای جدید می‌گیرد

## 🚀 ویژگی‌های PWA

### قابلیت‌ها:
- ✅ نصب روی دستگاه
- ✅ کار آفلاین
- ✅ Push notifications (در صورت پیکربندی)
- ✅ Background sync
- ✅ App-like experience

### سرویس ورکر:
فایل `sw.js` مدیریت:
- Cache کردن منابع
- کار آفلاین
- Background updates
- Push notifications

## 🔧 عیب‌یابی

### مشکلات متداول:

**خبرها لود نمی‌شوند:**
- اتصال اینترنت را چک کنید
- Console مرورگر را برای خطاها بررسی کنید
- API keys را درست وارد کرده باشید

**PWA نصب نمی‌شود:**
- از مرورگر مدرن استفاده کنید (Chrome, Edge, Safari)
- HTTPS نیاز است (برای production)
- Service Worker فعال باشد

**طراحی مشکل دارد:**
- CSS loading شده باشد
- فونت Vazirmatn درست لود شده باشد
- JavaScript بدون خطا اجرا شود

## 📄 فایل‌های پروژه

```
📦 ai-news-app/
├── 📄 index.html              # فایل اصلی اپلیکیشن
├── 📄 manifest.json           # PWA manifest
├── 📄 sw.js                   # Service Worker
├── 📄 ai-news-service.js      # سرویس گرفتن خبرها
├── 🖼️ icon-192.png            # آیکون 192x192
├── 🖼️ icon-512.png            # آیکون 512x512
└── 📄 README.md               # این فایل
```

## 🤝 مشارکت

برای بهبود اپلیکیشن:
1. Fork کنید
2. Feature branch بسازید
3. تغییرات را commit کنید
4. Pull request ایجاد کنید

## 📞 پشتیبانی

در صورت بروز مشکل:
1. Console مرورگر را چک کنید
2. مشکل را با جزئیات شرح دهید
3. اطلاعات مرورگر و سیستم‌عامل را ذکر کنید

---

**نسخه:** 1.0.0  
**تاریخ:** دسامبر 2025  
**توسط:** MiniMax Agent